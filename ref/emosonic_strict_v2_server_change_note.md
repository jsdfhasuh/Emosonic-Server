# EmoSonic strict-v2 服务端联调变更说明（已迁移）

当前服务端联调说明的唯一维护路径是：

`docs/emosonic_strict_v2_server_change_note.md`

本文仅用于旧链接跳转，不得在 `ref/` 下维护另一份正文。完整、唯一权威的
wire contract 仍为：

`specs/emosonic_strict_v2_socketio_server_contract.md`
